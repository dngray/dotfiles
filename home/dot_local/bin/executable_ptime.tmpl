#!/bin/sh

# Print timezones.
ptime() { printf "\e[36mTimezones\e[0m\n"; \
{{-  if .vaultid2 -}}
{{-   $tz := index (vault (printf "kv/users/%s/tz" .vaultid2)).data.data.tz -}}
{{-   range $tz }}
    printf "{{ .zone }} ({{ .note }})\n"; \
    TZ={{ .zone}} date;
{{-   end }} }
{{- end }}
