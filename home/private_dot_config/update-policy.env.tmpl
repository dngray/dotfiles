CONTAINERS="{{- range $index, $policy := (vault (printf "kv/shared/resources")).data.data.containerPolicy }}
{{- if $index }} {{end -}} {{ $policy  }}
{{- end }}"
REG_URL="{{ (vault "kv/shared/resources").data.data.containerRegistry }}"
